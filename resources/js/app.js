import './bootstrap';
import { createApp } from 'vue';

// ⚠️ ELIMINA o COMENTA estas líneas si las tienes:
// import App from './components/App.vue';
// createApp(App).mount('#app');

// ✅ USA ESTO EN SU LUGAR:
const app = createApp({});

// (Aquí registraremos componentes más adelante)

app.mount('#app');